[package]
name = "guest_wrapper"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = { workspace = true }
risc0-build-ethereum = "1.0.0"

[package.metadata.risc0]
methods = ["guest"]
