[package]
name = "version"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
vergen-gitcl = { workspace = true, features = ["build", "cargo", "si", "rustc"] }

[lints]
workspace = true
