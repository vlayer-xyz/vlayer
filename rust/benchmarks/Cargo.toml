[package]
name = "benchmarks"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = { workspace = true }
anyhow = { workspace = true }

[package.metadata.risc0]
methods = ["risc0_guest"]

[lints]
workspace = true
