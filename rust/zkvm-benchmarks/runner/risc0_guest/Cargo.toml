[workspace]


[package]
name = "risc0_benchmark_guest"
version = "0.1.0"
edition = "2021"


[dependencies]
risc0-zkvm = { version = "1.0.1", default-features = false, features = ["std"] }

zkvm-benchmarks-common = { path = "../../common" }
