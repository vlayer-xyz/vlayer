[package]
name = "call_guest_wrapper"
version = "0.1.0"
edition = "2021"

[build-dependencies]
guest_wrapper_utils = { path = "../../guest_wrapper_utils" }

[package.metadata.risc0]
methods = ["risc0_guest"]
