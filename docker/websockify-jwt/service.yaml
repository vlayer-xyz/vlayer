services:
  wsproxy-jwt:
    image: jwnmulder/websockify:0.12
    container_name: wsproxy-jwt
    platform: linux/amd64
    ports:
      - "127.0.0.1:3003:80" 
    volumes:
      - ../../fixtures/:/root:ro
    command: "80 --token-plugin JWTTokenApi --token-source ./jwt-authority.key.pub"
