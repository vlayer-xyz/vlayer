{
  "scripts": {
    "lint:solidity": "solhint './{vlayer,fixtures}/{src,script}/**/*.sol' --max-warnings 0 && forge fmt ./{vlayer,fixtures}/{src,script,test} --check"
  },
  "devDependencies": {
    "solhint": "^5.0.5"
  }
}
