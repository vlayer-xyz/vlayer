name: Test chain worker migration

# This workflow verifies that a new version of chain worker can
# successfully generate new ZK proofs based on existing proofs
# generated by the previous version.

on:
  pull_request:
    paths:
      - "rust/guest_wrapper/artifacts/chain_guest/elf_id"
  workflow_dispatch:

jobs:
  test-chain-worker-migration:
    name: Test chain worker migration
    runs-on: aws-linux-medium
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install Rust prerequisites
        id: rust_pre
        uses: ./.github/actions/rust-prerequisites

      - name: Install contracts prerequisites
        uses: ./.github/actions/contracts-prerequisites

      - name: Check clang and LLVM version
        env:
          EXPECTED_LLVM_VERSION: "18.1"
        run: bash/check-llvm-clang.sh

      - name: Download chain database
        run: |
          mkdir ./chain_db
          wget -O ./chain_db/mdbx.dat https://vlayer-development.s3.eu-north-1.amazonaws.com/chain_db/mdbx.dat

      - name: Build chain worker
        env:
          RISC0_USE_DOCKER: 1
          RUSTC_WRAPPER: ${{ steps.rust_pre.outputs.RUSTC_WRAPPER }}
        run: cargo build --bin worker

      - name: Run chain worker (append-prepend)
        env:
          RPC_URL: "https://dry-alpha-tab.optimism-sepolia.quiknode.pro/${{ secrets.QUICKNODE_API_KEY }}"
          CHAIN_ID: 11155420
          PROOF_MODE: fake
          RISC0_DEV_MODE: 1
          MODE: append-prepend
          DB_PATH: "./chain_db"
          START_BLOCK: 20000000
          MAX_BACK_PROPAGATION_BLOCKS: 1
          MAX_HEAD_BLOCKS: 1
          CONFIRMATIONS: 2
        run: cargo run --bin worker
